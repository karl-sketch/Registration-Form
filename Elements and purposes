body- The <body> element contains all the contents of an HTML document, such as headings, paragraphs, images, hyperlinks, tables, lists, etc.
 
label-The <label> HTML element represents a caption for an item in a user interface

Style - The HTML style attribute is used to add styles to an element, such as color, font, size, and more.

div- The <div> tag defines a division or a section in an HTML document

h1-The purpose of the <h1> tag in HTML is to define the main heading of a webpage. 
